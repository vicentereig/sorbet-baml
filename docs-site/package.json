{
  "name": "sorbet-baml-docs",
  "private": true,
  "scripts": {
    "build": "npm run frontend:build && bundle exec bridgetown build",
    "dev": "bundle exec bridgetown start",
    "frontend:build": "node esbuild.config.js --minify",
    "frontend:dev": "node esbuild.config.js --watch",
    "esbuild": "node esbuild.config.js --minify",
    "esbuild-dev": "node esbuild.config.js --watch"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.0",
    "autoprefixer": "^10.4.0",
    "esbuild": "^0.19.0",
    "glob": "^10.3.3",
    "postcss": "^8.4.0",
    "postcss-flexbugs-fixes": "^5.0.2",
    "postcss-import": "^15.1.0",
    "postcss-load-config": "^4.0.1",
    "postcss-preset-env": "^9.1.2",
    "read-cache": "^1.0.0",
    "tailwindcss": "^3.3.0"
  },
  "dependencies": {
    "plausible-tracker": "^0.3.9"
  }
}
